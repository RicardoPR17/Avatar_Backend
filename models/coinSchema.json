{
  "bsonType": "object",
  "title": "Coin Object Validation",
  "required": ["type", "owner"],
  "properties": {
    "type": { "bsonType": "string", "description": "The id of the user who owns the wallet" },
    "owner": { "bsonType": "string", "description": "The name of the particular crypto" }
  }
}
